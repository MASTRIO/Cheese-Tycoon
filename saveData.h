//// * Include
#include <iostream>
#include <fstream>

//// * Functions
// Cheese
void saveDataCheese() {
    std::ofstream saveFiles("./jagac/currency_cheese.cheese");
    saveFiles << cheese;
    saveFiles.close();
}

// Shiny Gems
void saveDataShinyGems() {
    std::ofstream saveFiles("./jagac/item_shiny_gems.cheese");
    saveFiles << shiny_gems;
    saveFiles.close();
}

// Cheese Jumpers
void saveDataCheeseJumpers() {
    std::ofstream saveFiles("./jagac/item_cheese_jumpers.cheese");
    saveFiles << cheese_jumpers;
    saveFiles.close();
}

// Blue Cheese
void saveDataBlueCheese() {
    std::ofstream saveFiles("./jagac/item_blue_cheese.cheese");
    saveFiles << blue_cheese;
    saveFiles.close();
}

// Pebbles
void saveDataPebbles() {
    std::ofstream saveFiles("./jagac/item_pebbles.cheese");
    saveFiles << pebbles;
    saveFiles.close();
}

// Sticks
void saveDataSticks() {
    std::ofstream saveFiles("./jagac/item_sticks.cheese");
    saveFiles << sticks;
    saveFiles.close();
}

// Logs
void saveDataLogs() {
    std::ofstream saveFiles("./jagac/item_logs.cheese");
    saveFiles << logs;
    saveFiles.close();
}

// Ruby
void saveDataRuby() {
    std::ofstream saveFiles("./jagac/item_ruby.cheese");
    saveFiles << ruby;
    saveFiles.close();
}

// Leaf
void saveDataLeaf() {
    std::ofstream saveFiles("./jagac/item_leaf.cheese");
    saveFiles << leaf;
    saveFiles.close();
}

// Carrots
void saveDataCarrots() {
    std::ofstream saveFiles("./jagac/item_carrots.cheese");
    saveFiles << carrots;
    saveFiles.close();
}

// Potato
void saveDataPotato() {
    std::ofstream saveFiles("./jagac/item_potato.cheese");
    saveFiles << potato;
    saveFiles.close();
}

// Bread
void saveDataBread() {
    std::ofstream saveFiles("./jagac/item_bread.cheese");
    saveFiles << bread;
    saveFiles.close();
}

// Brick
void saveDataBrick() {
    std::ofstream saveFiles("./jagac/item_brick.cheese");
    saveFiles << brick;
    saveFiles.close();
}

// Mouldy Potato
void saveDataMouldyPotato() {
    std::ofstream saveFiles("./jagac/item_mouldy_potato.cheese");
    saveFiles << mouldy_potato;
    saveFiles.close();
}

// Child
void saveDataChild() {
    std::ofstream saveFiles("./jagac/item_child.cheese");
    saveFiles << child;
    saveFiles.close();
}

// Toy Bear
void saveDataToyBear() {
    std::ofstream saveFiles("./jagac/item_toy_bear.cheese");
    saveFiles << toy_bear;
    saveFiles.close();
}

// Moose
void saveDataMoose() {
    std::ofstream saveFiles("./jagac/item_moose.cheese");
    saveFiles << moose;
    saveFiles.close();
}